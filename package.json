{
  "name": "bharatone",
  "private": true,
  "version": "1.0.0",
  "description": "India Metro Systems — Interactive Metro Maps",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
